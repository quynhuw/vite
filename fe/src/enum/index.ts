export enum SeatStatus {
  available = 1,
  selected = 2,
  booked = 0,
  unavailable = 3,
}
export enum UserRole {
  admin = 1,
  user = 0,
}
export enum BookingStatus {
  pending = 0,
  success = 1,
  cancel = 2,
}
export enum ShowtimeStatus {
  delete = 0,
  active = 1,
  cancel = 2,
  screened = 3,
}
